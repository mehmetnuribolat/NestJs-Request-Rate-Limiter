import { SwaggerConfig } from './swagger.interface';

export const SWAGGER_CONFIG: SwaggerConfig = {
  title: 'Request Rate Limiter Documentation',
  description: 'Api Services for Public/Private EndPoints',
  version: '1.0',
  tags: ['public', 'private'],
};
